import React from 'react';
import { View } from 'react-native';
import { HomePage } from './pages';

export default App = ()=> {
  return (
    <View style={{flex: 1}}>
      <HomePage />
    </View>
  );
};
