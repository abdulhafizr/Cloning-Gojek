import React from 'react';
import { View } from 'react-native';
import { HomePage } from './containers/pages';

export default App = ()=> {
  return (
    <View style={{flex: 1}}>
      <HomePage />
    </View>
  );
};
